name: Publish To JetBrains Marketplace

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: Publish Plugin
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Verify Marketplace token
        run: |
          if [ -z "${ORG_GRADLE_PROJECT_intellijPlatformPublishingToken}" ] && [ -z "${PUBLISH_TOKEN}" ]; then
            echo "Marketplace token secret is required for publishPlugin."
            echo "Provide either ORG_GRADLE_PROJECT_intellijPlatformPublishingToken or PUBLISH_TOKEN."
            exit 1
          fi
        env:
          ORG_GRADLE_PROJECT_intellijPlatformPublishingToken: ${{ secrets.ORG_GRADLE_PROJECT_intellijPlatformPublishingToken }}
          PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}

      - name: Build and verify plugin
        run: ./gradlew --no-daemon test buildPlugin verifyPlugin --stacktrace

      - name: Publish plugin
        run: ./gradlew --no-daemon publishPlugin --stacktrace
        env:
          ORG_GRADLE_PROJECT_intellijPlatformPublishingToken: ${{ secrets.ORG_GRADLE_PROJECT_intellijPlatformPublishingToken }}
          PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
          CERTIFICATE_CHAIN: ${{ secrets.CERTIFICATE_CHAIN }}
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          PRIVATE_KEY_PASSWORD: ${{ secrets.PRIVATE_KEY_PASSWORD }}
